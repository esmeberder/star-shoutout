<p>Star's name: <%= @user.name %></p>
<p>Star's tags</p>
<p>Star's bio: <%= @user.bio %></p>
<%= cl_image_tag @user.photo, height: 300, width: 400, crop: :fill %>
<p>------------------------------</p>
<h3>Services:</h3>
<% @services.each do |service| %>
  <%= link_to "Book #{service.title} #{service.price} RMB", new_service_booking_path(service) %>
  <% if policy(service).edit? %>
    <%= link_to "Update", edit_service_path(service) %>
  <% end %>
  <% if policy(service).destroy? %>
    <%= link_to "Delete", service_path(service), method: :delete, data: { confirm: "Are you sure?" } %>
  <% end %>
  <p>------------------------------</p>
<% end %>
<p>-----------------------------------</p>
<h1>Lastest Shoutouts</h1>
<p>Seed with samples</p>
<h1>Related</h1>
<p>Show some other stars</p>
<%= link_to "View my bookings", service_bookings_path(@bookings) %>
<% if @user == current_user %>
  <%= link_to "Add a new service", new_service_path %>
<% end %>
<%= link_to "Back to all stars", users_path %>